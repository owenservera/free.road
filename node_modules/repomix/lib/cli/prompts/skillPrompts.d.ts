import fs from 'node:fs/promises';
import * as prompts from '@clack/prompts';
export type SkillLocation = 'personal' | 'project';
export interface SkillPromptResult {
    location: SkillLocation;
    skillDir: string;
}
/**
 * Get the base directory for skills based on location type.
 */
export declare const getSkillBaseDir: (cwd: string, location: SkillLocation) => string;
/**
 * Prompt user for skill location and handle overwrite confirmation.
 */
export declare const promptSkillLocation: (skillName: string, cwd: string, deps?: {
    select: <Value>(opts: prompts.SelectOptions<Value>) => Promise<symbol | Value>;
    confirm: (opts: prompts.ConfirmOptions) => Promise<boolean | symbol>;
    isCancel: typeof prompts.isCancel;
    access: typeof fs.access;
}) => Promise<SkillPromptResult>;
//# sourceMappingURL=skillPrompts.d.ts.map