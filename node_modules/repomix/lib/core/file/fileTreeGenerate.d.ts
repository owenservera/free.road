export interface TreeNode {
    name: string;
    children: TreeNode[];
    isDirectory: boolean;
}
export declare const generateFileTree: (files: string[], emptyDirPaths?: string[]) => TreeNode;
export declare const treeToString: (node: TreeNode, prefix?: string) => string;
/**
 * Converts a tree to string with line counts for files.
 * @param node The tree node to convert
 * @param lineCounts Map of file paths to line counts
 * @param prefix Current indentation prefix
 * @param currentPath Current path being built (for looking up line counts)
 */
export declare const treeToStringWithLineCounts: (node: TreeNode, lineCounts: Record<string, number>, prefix?: string, currentPath?: string) => string;
export declare const generateTreeString: (files: string[], emptyDirPaths?: string[]) => string;
export declare const generateTreeStringWithLineCounts: (files: string[], lineCounts: Record<string, number>, emptyDirPaths?: string[]) => string;
//# sourceMappingURL=fileTreeGenerate.d.ts.map